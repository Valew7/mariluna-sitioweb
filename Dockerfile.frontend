# Dockerfile para servir el frontend como archivos estáticos
FROM nginx:alpine

# Copiar archivos del frontend al directorio de Nginx
COPY frontend/ /usr/share/nginx/html/

# Exponer el puerto por defecto de Nginx
EXPOSE 80
